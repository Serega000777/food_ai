// Domain tables land here starting with User/UserProfile/Goal in Phase 1
// (see docs/decisions and master prompt §6 Domain Model). Kept empty for now so
// `drizzle({ schema })` and drizzle-kit have a valid, honestly-empty schema module
// instead of a placeholder table nothing references yet.
export {};
