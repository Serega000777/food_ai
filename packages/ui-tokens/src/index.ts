/** Animation durations in ms, mirroring the `--motion-*` CSS custom properties in
 * tokens.css, for the rare case JS needs the number (e.g. a transition callback). */
export const MOTION_FAST_MS = 200;
export const MOTION_BASE_MS = 300;
